// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("mycrypt", "for mycrypt support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("mycrypt", "enable mycrypt support", "no");

if (PHP_MYCRYPT != "no") {
	EXTENSION("mycrypt", "mycrypt.c");
}

